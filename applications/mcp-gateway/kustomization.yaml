---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - 00-namespaces.yaml
  - 01-subscription.yaml
  - 02-gateway.yaml
  - 03-reference-grant.yaml
  - 04-mcp-gateway-extension.yaml
  # 08-gateway-route.yaml carries a <GATEWAY_PUBLIC_HOST> placeholder — it is
  # rendered and applied by install.sh, not by kustomize.
  # Test resources — remove for production
  - 05-test-namespace.yaml
  - 06-test-configmap.yaml
  - 07-test-mcp-server.yaml
